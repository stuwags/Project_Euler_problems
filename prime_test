def prime_test(number)
  iterator = number / 2; prime_falsified = false
  while iterator > 1 && prime_falsified == false
    prime_test = number%iterator
    iterator -= 1
    print "hi"
    if iterator == 1
      @prime_number = 7
      puts @prime_number
    elsif prime_test == 0
      prime_falsified = true
    else
      puts "prime test continues @ #{prime_test_iterator}"     
    end
  end  
end